name: 'Hexo Action'
author: 'smallblack'
description: 'Hexo CI action for deployment.'
inputs:
  cmd:
    description: 'Hexo command'
    required: true
    default: 'help'
  user_name:
    description: 'Github User Name'
    required: false
    default: 'username'
  user_email:
    description: 'Github User Email'
    required: false
    default: 'username@example.com'
outputs:
  notify: # id of output
    description: 'The notification of deployment result'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.cmd }}
    - ${{ inputs.user_name }}
    - ${{ inputs.user_email }}
branding:
  icon: 'hexagon'  
  color: 'blue'
